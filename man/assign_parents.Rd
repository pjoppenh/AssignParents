% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_parents.R
\name{assign_parents}
\alias{assign_parents}
\title{Assign DIRTY ASVs to CLEAN parents}
\usage{
assign_parents(
  clean_tab,
  dirty_tab,
  clean_seqs,
  dirty_seqs,
  min_identity = 0.98,
  min_presence_score = 0,
  presence_cutoff = 1,
  min_recall = 0.5,
  min_n = 6,
  keep_unassigned = TRUE,
  presence_weights = c(phi = 0.5, ppmi = 0.3, f1 = 0.2),
  abundance_weights = c(spearman = 0.6, hellinger = 0.4),
  single_weights = c(presence = 0.6, abundance = 0.4),
  single_scale_method = c("z", "robust", "rank"),
  enforce_dominance = TRUE,
  dominance_mean_ratio_max = 0.9,
  dominance_frac_exceed_max = 0.35,
  dominance_min_both = 3,
  identity_neighborhood_mismatches = 1L,
  neighborhood_z_margin = 1.4,
  n_cores = 1L,
  parallel_method = c("auto", "fork", "cluster", "none")
)
}
\arguments{
\item{clean_tab}{Numeric matrix of CLEAN ASVs (rows = ASVs, cols = samples)}

\item{dirty_tab}{Numeric matrix of DIRTY ASVs (rows = ASVs, cols = samples)}

\item{clean_seqs}{Named character vector of CLEAN sequences}

\item{dirty_seqs}{Named character vector of DIRTY sequences}

\item{min_identity}{Minimum percent identity required}

\item{min_presence_score}{Minimum presence composite threshold}

\item{presence_cutoff}{Abundance threshold for presence/absence binarization}

\item{min_recall}{Minimum recall required in presence composite}

\item{min_n}{Minimum co-occurrence sample size}

\item{keep_unassigned}{Logical; keep DIRTY ASVs with no parent}

\item{presence_weights}{Named numeric vector for presence composite}

\item{abundance_weights}{Named numeric vector for abundance composite}

\item{single_weights}{Named numeric vector for final meta-score}

\item{single_scale_method}{Scaling method for row-wise normalization}

\item{enforce_dominance}{Logical; enforce dominance filtering}

\item{dominance_mean_ratio_max}{Maximum allowed normalized mean ratio}

\item{dominance_frac_exceed_max}{Maximum allowed exceedance fraction}

\item{dominance_min_both}{Minimum shared samples for dominance test}

\item{identity_neighborhood_mismatches}{Neighborhood size in mismatches}

\item{neighborhood_z_margin}{Z-score margin required to override identity}

\item{n_cores}{Number of cores for parallelization}

\item{parallel_method}{Parallel backend. "auto" chooses "fork" on macOS/Linux
and "cluster" on Windows when n_cores > 1; otherwise "none".}
}
\value{
A list containing collapsed tables, mapping, score matrices,
        dominance diagnostics, and matched/unmatched sequence vectors.
}
\description{
Collapses DIRTY ASVs into CLEAN ASVs using sequence identity,
prevalence-aware presence similarity, abundance similarity,
dominance filters, and a standardized meta-score.
}
